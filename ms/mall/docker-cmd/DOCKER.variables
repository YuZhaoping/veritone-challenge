#!/usr/bin/env bash

IMAGE_TAG="0.1.0"
IMAGE_REPOSITORY="veritone/ms/mall-svc"

IMAGE_FULL_NAME="${IMAGE_REPOSITORY}:${IMAGE_TAG}"

CONTAINER_NAME=veritone-mall-svc

# Network names that used for docker deployment.
BACKEND_NETWORK_NAME=app-back-net

# Define the container using ports.
PORT=3000
HOST_PORT=3000
