import React from 'react';

import { TestComponent } from '../../components/tests';

const ShoppingList = () => {
  return (
    <React.Fragment>
      <TestComponent />
    </React.Fragment>
  );
};

export default ShoppingList;
